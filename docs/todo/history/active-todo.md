# Block Editor 当前主 TODO（唯一执行入口）

> 维护迁移说明（2026-03-28）：后续统一维护 `docs/todo/current.md`，本文件仅保留历史拷贝，不再继续更新。

最后更新：2026-03-28  
状态：进行中  
说明：后续所有任务只在本文件维护与推进，历史清单仅归档不再继续勾选。

## 1. 当前未完成项目总览

### P0 - 图片交互与拖拽一致性收口（进行中）

来源：`docs/todo/history/image-interaction-dnd-todo.md`（历史来源，不再继续维护）

- [ ] P0-1 固化复现脚本（3类问题：图片点击异常、预览样式异常、非图片块长按拖拽失败）。
- [ ] P0-2 建立块类型拖拽矩阵（段落/标题/引用/代码块/列表/Callout/表格/图片）。
- [ ] P0-3 输出并确认图片交互状态机（`idle -> selected -> toolbar-open -> preview-open`）。
- [ ] P0-4 处理 caption 编辑与 block-handle 抢焦点/抢 hover 冲突。
- [ ] P0-5 完成“长按拖拽阈值”规则（时间阈值 + 位移阈值）。
- [ ] P0-6 统一拖拽落点反馈样式（段落/引用/代码块/表格等一致高亮）。
- [ ] P0-7 统一拖拽结束清理（handle/drop-target/selection/ghost）。
- [ ] P0-8 修复“拖拽后重复插入图片/文本”回归问题并稳定复现验证。
- [ ] P0-9 补齐 e2e：图片点击状态机、预览弹层、长按拖拽矩阵、原生拖拽干扰防回归。
- [ ] P0-10 更新行为基准与评分卡（新增 H4 系列项）并完成一轮验收报告。

### P1 - 协作后端最小闭环（未开始）

来源：`docs/todo/history/toolbar-benchmark-plan.md`（任务 1/2）

- [ ] P1-1 协作后端接入与鉴权方案落地（基础架构、会话鉴权、最小可运行链路）。
- [ ] P1-2 评论与版本历史服务端化最小闭环（写入、查询、回放、失败兜底）。

## 2. 本轮执行顺序（建议）

1. 先完成 `P0-1 ~ P0-4`（把行为与状态边界定义清楚，避免后续反复返工）。
2. 再完成 `P0-5 ~ P0-8`（拖拽核心逻辑与回归问题一次性收口）。
3. 然后完成 `P0-9 ~ P0-10`（自动化与评分补齐，形成可发布证据）。
4. 最后推进 `P1-1 ~ P1-2`（进入协作后端阶段）。

## 3. 执行规范入口

执行工作流标准已独立维护在：

- `docs/developer/execution-workflow-standard.md`

后续本文件仅维护“任务清单 + 执行日志”，不再重复维护流程规范正文。

## 4. 执行日志

## 执行日志（2026-03-28 16:30）
- 任务ID：流程治理初始化
- 状态变更：todo -> done
- 代码改动：创建本主 TODO，汇总未完成项目，定义执行工作流标准。
- 验证命令：结构整理类改动，无需构建验证。
- 验证结果：目录与文档创建完成。
- 文档更新：本文件新增。
- 风险与待办：下一轮从 `P0-1` 开始推进。

## 执行日志（2026-03-28 16:55）
- 任务ID：流程标准文档重构
- 状态变更：doing -> done
- 代码改动：将执行标准重写为“维护流程模板文档”，明确目标定义/执行规则/阶段拆分/验收标准/执行顺序/行为项模板/执行日志模板，并新增后续 AI 可直接复用的执行提示。
- 验证命令：`pnpm --dir docs docs:build`
- 验证结果：通过。
- 文档更新：`docs/developer/execution-workflow-standard.md`
- 风险与待办：后续新需求统一按该模板先产出 TODO 再进入开发。

## 执行日志（2026-03-28 17:30）
- 任务ID：P0 图片交互与预览体验收口（第一批）
- 状态变更：doing -> review
- 代码改动：修复图片点击选中链路（NodeSelection 回收与焦点同步）、图片工具栏从布局流改为绝对定位、图片选中态只包裹图片区域；图片 caption 暂时关闭；预览弹层补滚轮缩放与指针拖拽平移并提升跟手性与可视区占比；列表样式补齐（有序/无序 marker 与间距）；样式按领域拆分为 `styles/image.css` 与 `styles/list.css`。
- 验证命令：`pnpm --filter @block-editor/editor test`、`pnpm --filter block-editor-playground build`、`pnpm --filter block-editor-playground test:e2e`
- 验证结果：通过（22/22、build 通过、e2e 39/39）。
- 文档更新：`docs/developer/call-chains.md`、`docs/usage/regression-checklist.md`、`docs/developer/theme-and-ui.md`
- 风险与待办：caption 功能为临时关闭策略，后续若恢复需补回对应 e2e 断言与使用文档说明。
