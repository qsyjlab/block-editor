# 文档格式兼容性测试样本

这份文档用于验证编辑器对飞书 / 语雀常见文档格式的支持情况。导入后请检查标题、段落、行内样式、列表、任务、引用、Callout、代码块、表格、图片、链接和分割线是否保持可读。

## 1. 基础文本样式

这是一段普通正文，包含 **加粗**、*斜体*、~~删除线~~、`行内代码`、<u>下划线</u>、上标 x<sup>2</sup>、下标 H<sub>2</sub>O。

这是一段包含链接的正文：[OpenAI](https://openai.com) 和 [块内跳转示例](#3-列表与任务)。

[indent:1] 这是一段一级缩进文本，用于验证 Markdown 导入时的缩进恢复。

[indent:2] 这是一段二级缩进文本，用于验证多级缩进是否可读。

## 2. 标题层级

### 2.1 三级标题

普通段落位于三级标题下方。

#### 2.1.1 四级标题

普通段落位于四级标题下方。

##### 2.1.1.1 五级标题

普通段落位于五级标题下方。

###### 2.1.1.1.1 六级标题

普通段落位于六级标题下方。

## 3. 列表与任务

### 无序列表

- 一级无序项 A
  - 二级无序项 A-1
  - 二级无序项 A-2，包含 **加粗**
- 一级无序项 B

### 有序列表

1. 第一步：创建测试文档
2. 第二步：导入编辑器
3. 第三步：检查结构和样式
   1. 子步骤：检查嵌套编号
   2. 子步骤：检查缩进

### 任务列表

- [ ] 未完成任务：检查飞书复制粘贴
- [x] 已完成任务：检查 Markdown 导入
- [ ] 未完成任务：检查 DOCX 导出后再导入

## 4. 引用与 Callout

> 这是一段普通引用。用于验证语雀 / 飞书中的引用块粘贴或导入后是否仍然有明确的块级语义。
>
> 引用中包含第二段内容。

> [!INFO]
> 这是一个信息 Callout。用于验证 `[!INFO]` 风格是否能导入为信息块。

> [!TIP]
> 这是一个成功 / 提示 Callout。用于验证 TIP 是否能归一到 success 类型。

> [!WARNING]
> 这是一个警告 Callout。用于验证 warning 样式。

> [!DANGER]
> 这是一个危险 Callout。用于验证 danger 样式。

## 5. 代码块

下面是 TypeScript 代码块：

~~~ts
type DocumentStatus = 'draft' | 'review' | 'published'

interface Article {
  id: string
  title: string
  status: DocumentStatus
  tags: string[]
}

const article: Article = {
  id: 'doc-001',
  title: '格式兼容性测试',
  status: 'review',
  tags: ['feishu', 'yuque', 'markdown'],
}

console.log(article.title)
~~~

下面是 JSON 代码块：

~~~json
{
  "title": "格式兼容性测试",
  "source": ["feishu", "yuque"],
  "features": {
    "table": true,
    "callout": true,
    "taskList": true
  }
}
~~~

## 6. 表格

| 模块 | 样式能力 | 预期结果 | 验收状态 |
| --- | --- | --- | --- |
| 标题 | H1-H6 | 层级可见，大纲可识别 | 待验证 |
| 行内样式 | 加粗 / 斜体 / 删除线 / 链接 | 样式不丢失 | 待验证 |
| 列表 | 无序 / 有序 / 任务 | 层级和勾选状态保留 | 待验证 |
| 表格 | 普通表格 | 行列结构可读 | 待验证 |
| 代码块 | 多语言代码 | 内容和语言可读 | 待验证 |

## 7. 图片

下面是一张远程图片，用于验证 Markdown 图片导入和导出：

![Block Editor 测试图片](https://placehold.co/640x320/png?text=Block+Editor+Format+Test)

图片下方普通段落，用于检查图片前后段落是否保持连续。

## 8. 分割线

分割线上方内容。

---

分割线下方内容。

## 9. 混合内容段落

这一段用于混合测试：请检查 **加粗文本**、*斜体文本*、`行内代码`、[链接](https://example.com)、~~删除线~~ 是否可以在同一段中共存。

## 10. 验收记录

| 测试来源 | 结果 | 失败项 | 备注 |
| --- | --- | --- | --- |
| Markdown 导入 | 通过 / 部分通过 / 失败 |  |  |
| Markdown 导出后再导入 | 通过 / 部分通过 / 失败 |  |  |
| 飞书复制粘贴 | 通过 / 部分通过 / 失败 |  |  |
| 语雀复制粘贴 | 通过 / 部分通过 / 失败 |  |  |
| DOCX 导入 / 导出 | 通过 / 部分通过 / 失败 |  |  |
