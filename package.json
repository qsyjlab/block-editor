{
  "name": "block-editor-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
